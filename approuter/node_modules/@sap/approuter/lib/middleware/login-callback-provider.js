'use strict';

module.exports = {
  isLoginCallback: function (req) {
    return (req.url.indexOf(req.routerConfig.appConfig.login.callbackEndpoint) >= 0 && req.method === 'GET' && !(req.session && req.session.user));
  }
};